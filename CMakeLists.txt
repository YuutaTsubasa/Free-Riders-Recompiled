cmake_minimum_required(VERSION 3.20)
cmake_policy(SET CMP0091 NEW)
set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
project(FreeRidersRecompiled LANGUAGES C CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
if(ANDROID OR APPLE)
    # libc++ (the NDK's, and LLVM's on macOS) still ships std::jthread and
    # std::stop_token as experimental.
    add_compile_options($<$<COMPILE_LANGUAGE:CXX>:-fexperimental-library>)
    add_link_options(-fexperimental-library)
endif()
if(APPLE)
    # The pinned fmt uses malloc without including <cstdlib>, which newer
    # libc++ no longer brings in on its own.
    add_compile_options("$<$<COMPILE_LANGUAGE:CXX>:SHELL:-include cstdlib>")
endif()
option(SFR_BUILD_DIAGNOSTIC "Build the game CPU diagnostic (requires generated code)" OFF)
if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tools/XenonRecomp/CMakeLists.txt")
    message(FATAL_ERROR "Run scripts/bootstrap.py to obtain the pinned XenonRecomp tools.")
endif()
add_subdirectory(tools/XenonRecomp)
# Decoding the verified XEX and installing from a disc image or folder.
add_library(sfr_installer STATIC src/xex_image.cpp src/installer.cpp)
target_include_directories(sfr_installer PUBLIC src PRIVATE tools/XenonRecomp/thirdparty/TinySHA1)
target_link_libraries(sfr_installer PRIVATE XenonUtils)
add_executable(sfr_image_dump src/image_dump.cpp)
target_link_libraries(sfr_image_dump PRIVATE sfr_installer)
include(CTest)
# Host platform: Windows kernel objects and threads, or their POSIX
# counterparts (portable_waitables emulates the objects).
if(WIN32)
    set(SFR_NATIVE_THREAD src/native_thread.cpp)
    set(SFR_PORTABLE_WAITABLES)
    set(SFR_ASSET_FILES src/asset_files.cpp)
else()
    set(SFR_NATIVE_THREAD src/native_thread_posix.cpp src/portable_waitables.cpp)
    set(SFR_PORTABLE_WAITABLES src/portable_waitables.cpp)
    set(SFR_ASSET_FILES src/asset_files_posix.cpp)
    # Window, input and sound on Linux and Android.
    if(ANDROID)
        # Android has no system SDL2: SDL 2.32.8 from source (tools/SDL),
        # shared, because its Java activity loads libSDL2.so, then libmain.so.
        if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tools/SDL/CMakeLists.txt")
            message(FATAL_ERROR "Run scripts/bootstrap.py to obtain the pinned SDL.")
        endif()
        set(SDL_SHARED ON CACHE BOOL "" FORCE)
        set(SDL_STATIC OFF CACHE BOOL "" FORCE)
        set(SDL_TEST OFF CACHE BOOL "" FORCE)
        set(SDL2_DISABLE_SDL2MAIN ON CACHE BOOL "" FORCE)
        add_subdirectory(tools/SDL EXCLUDE_FROM_ALL)
        set(SDL2_VERSION 2.32.8)
    else()
        find_package(SDL2 REQUIRED)
    endif()
endif()
add_library(sfr_guest_execution STATIC src/guest_execution.cpp)
find_package(Threads REQUIRED)
target_link_libraries(sfr_guest_execution PUBLIC Threads::Threads)
target_include_directories(sfr_guest_execution PUBLIC src)
add_executable(sfr_guest_execution_test tests/guest_execution_test.cpp)
target_link_libraries(sfr_guest_execution_test PRIVATE sfr_guest_execution)
add_test(NAME guest_execution COMMAND sfr_guest_execution_test)
set_tests_properties(guest_execution PROPERTIES TIMEOUT 15)
add_executable(sfr_memory_test tests/guest_memory_test.cpp src/guest_memory.cpp)
target_include_directories(sfr_memory_test PRIVATE src)
add_test(NAME guest_memory COMMAND sfr_memory_test)
add_executable(sfr_pending_guest_write_test tests/pending_guest_write_test.cpp src/guest_memory.cpp)
target_include_directories(sfr_pending_guest_write_test PRIVATE src)
add_test(NAME pending_guest_write COMMAND sfr_pending_guest_write_test)
add_executable(sfr_debug_monitor_test tests/debug_monitor_test.cpp src/debug_monitor.cpp src/guest_memory.cpp)
target_include_directories(sfr_debug_monitor_test PRIVATE src)
add_test(NAME debug_monitor COMMAND sfr_debug_monitor_test)
add_executable(sfr_nui_device_status_test tests/nui_device_status_test.cpp src/nui_device_status.cpp src/guest_memory.cpp)
target_include_directories(sfr_nui_device_status_test PRIVATE src)
add_test(NAME nui_device_status COMMAND sfr_nui_device_status_test)
add_executable(sfr_nui_skeleton_test tests/nui_skeleton_test.cpp src/nui_skeleton.cpp src/nui_device_status.cpp src/guest_memory.cpp)
target_include_directories(sfr_nui_skeleton_test PRIVATE src)
add_test(NAME nui_skeleton COMMAND sfr_nui_skeleton_test)
add_executable(sfr_nui_speech_test tests/nui_speech_test.cpp src/nui_speech.cpp src/guest_memory.cpp)
target_include_directories(sfr_nui_speech_test PRIVATE src)
add_test(NAME nui_speech COMMAND sfr_nui_speech_test)
add_executable(sfr_nui_race_test tests/nui_race_test.cpp src/nui_race.cpp src/guest_memory.cpp)
target_include_directories(sfr_nui_race_test PRIVATE src)
add_test(NAME nui_race COMMAND sfr_nui_race_test)
add_executable(sfr_shader_inputs_test tests/shader_inputs_test.cpp src/shader_inputs.cpp)
target_include_directories(sfr_shader_inputs_test PRIVATE src)
add_test(NAME shader_inputs COMMAND sfr_shader_inputs_test)
add_executable(sfr_loop_constants_test tests/loop_constants_test.cpp src/loop_constants.cpp)
target_include_directories(sfr_loop_constants_test PRIVATE src)
add_test(NAME loop_constants COMMAND sfr_loop_constants_test)
add_executable(sfr_vertex_palette_test tests/vertex_palette_test.cpp src/vertex_palette.cpp)
target_include_directories(sfr_vertex_palette_test PRIVATE src)
add_test(NAME vertex_palette COMMAND sfr_vertex_palette_test)
add_executable(sfr_unselected_users_test tests/unselected_users_test.cpp src/unselected_users.cpp src/guest_memory.cpp)
target_include_directories(sfr_unselected_users_test PRIVATE src)
add_test(NAME unselected_users COMMAND sfr_unselected_users_test)
add_executable(sfr_content_files_test tests/content_files_test.cpp src/content_files.cpp src/guest_memory.cpp)
target_include_directories(sfr_content_files_test PRIVATE src)
add_test(NAME content_files COMMAND sfr_content_files_test)
add_executable(sfr_multibyte_unicode_test tests/multibyte_unicode_test.cpp src/multibyte_unicode.cpp src/guest_memory.cpp)
target_include_directories(sfr_multibyte_unicode_test PRIVATE src)
add_test(NAME multibyte_unicode COMMAND sfr_multibyte_unicode_test)
add_executable(sfr_render_state_entries_test tests/render_state_entries_test.cpp)
target_include_directories(sfr_render_state_entries_test PRIVATE src)
add_test(NAME render_state_entries COMMAND sfr_render_state_entries_test)
add_executable(sfr_video_globals_test tests/video_globals_test.cpp src/video_globals.cpp src/guest_memory.cpp)
target_include_directories(sfr_video_globals_test PRIVATE src)
add_test(NAME video_globals COMMAND sfr_video_globals_test)
add_executable(sfr_counted_branch_test tests/counted_branch_test.cpp)
target_include_directories(sfr_counted_branch_test PRIVATE src)
add_test(NAME counted_branch COMMAND sfr_counted_branch_test)
add_executable(sfr_image_protection_test tests/image_protection_test.cpp src/image_protection.cpp src/guest_memory.cpp)
target_include_directories(sfr_image_protection_test PRIVATE src)
add_test(NAME image_protection COMMAND sfr_image_protection_test)
add_executable(sfr_xex_module_test tests/xex_module_test.cpp src/xex_module.cpp src/guest_memory.cpp)
target_include_directories(sfr_xex_module_test PRIVATE src)
add_test(NAME xex_module COMMAND sfr_xex_module_test)
add_executable(sfr_virtual_memory_test tests/virtual_memory_test.cpp src/virtual_memory.cpp src/guest_memory.cpp)
target_include_directories(sfr_virtual_memory_test PRIVATE src)
add_test(NAME virtual_memory COMMAND sfr_virtual_memory_test)
add_executable(sfr_critical_section_test tests/critical_section_test.cpp src/critical_section.cpp src/guest_memory.cpp)
target_include_directories(sfr_critical_section_test PRIVATE src)
add_test(NAME critical_section COMMAND sfr_critical_section_test)
add_executable(sfr_hardware_info_test tests/hardware_info_test.cpp src/hardware_info.cpp src/guest_memory.cpp)
target_include_directories(sfr_hardware_info_test PRIVATE src)
add_test(NAME hardware_info COMMAND sfr_hardware_info_test)
add_executable(sfr_thread_local_storage_test tests/thread_local_storage_test.cpp src/thread_local_storage.cpp src/guest_memory.cpp)
target_include_directories(sfr_thread_local_storage_test PRIVATE src)
add_test(NAME thread_local_storage COMMAND sfr_thread_local_storage_test)
add_executable(sfr_system_time_test tests/system_time_test.cpp src/system_time.cpp src/guest_memory.cpp)
target_include_directories(sfr_system_time_test PRIVATE src)
add_test(NAME system_time COMMAND sfr_system_time_test)
add_executable(sfr_system_config_test tests/system_config_test.cpp src/system_config.cpp src/guest_memory.cpp)
target_include_directories(sfr_system_config_test PRIVATE src)
add_test(NAME system_config COMMAND sfr_system_config_test)
add_executable(sfr_native_language_test tests/native_language_test.cpp src/native_language.cpp src/guest_memory.cpp)
target_include_directories(sfr_native_language_test PRIVATE src)
add_test(NAME native_language COMMAND sfr_native_language_test)
add_executable(sfr_native_country_test tests/native_country_test.cpp src/native_country.cpp src/guest_memory.cpp)
target_include_directories(sfr_native_country_test PRIVATE src)
add_test(NAME native_country COMMAND sfr_native_country_test)
add_executable(sfr_system_country_test tests/system_country_test.cpp src/system_config.cpp src/guest_memory.cpp)
target_include_directories(sfr_system_country_test PRIVATE src)
add_test(NAME system_country COMMAND sfr_system_country_test)
add_executable(sfr_guest_critical_sections_test tests/guest_critical_sections_test.cpp src/guest_critical_sections.cpp src/critical_section.cpp src/guest_memory.cpp)
target_include_directories(sfr_guest_critical_sections_test PRIVATE src)
target_link_libraries(sfr_guest_critical_sections_test PRIVATE sfr_guest_execution)
add_test(NAME guest_critical_sections COMMAND sfr_guest_critical_sections_test)
set_tests_properties(guest_critical_sections PROPERTIES TIMEOUT 20)
add_executable(sfr_game_region_test tests/game_region_test.cpp src/game_region.cpp src/guest_memory.cpp)
target_include_directories(sfr_game_region_test PRIVATE src)
add_test(NAME game_region COMMAND sfr_game_region_test)
add_executable(sfr_guest_clock_test tests/guest_clock_test.cpp src/guest_clock.cpp src/guest_memory.cpp)
target_include_directories(sfr_guest_clock_test PRIVATE src)
add_test(NAME guest_clock COMMAND sfr_guest_clock_test)
add_executable(sfr_timestamp_bundle_test tests/timestamp_bundle_test.cpp src/timestamp_bundle.cpp src/guest_memory.cpp)
target_include_directories(sfr_timestamp_bundle_test PRIVATE src)
add_test(NAME timestamp_bundle COMMAND sfr_timestamp_bundle_test)
add_executable(sfr_vector_memory_test tests/vector_memory_test.cpp src/vector_memory.cpp src/guest_memory.cpp)
target_include_directories(sfr_vector_memory_test PRIVATE src)
add_test(NAME vector_memory COMMAND sfr_vector_memory_test)
add_executable(sfr_optional_import_policy_test tests/optional_import_policy_test.cpp src/optional_import_policy.cpp)
target_include_directories(sfr_optional_import_policy_test PRIVATE src)
add_test(NAME optional_import_policy COMMAND sfr_optional_import_policy_test)
add_executable(sfr_native_modules_test tests/native_modules_test.cpp src/native_modules.cpp src/guest_memory.cpp)
target_include_directories(sfr_native_modules_test PRIVATE src)
add_test(NAME native_modules COMMAND sfr_native_modules_test)
add_executable(sfr_integer_arithmetic_test tests/integer_arithmetic_test.cpp src/integer_arithmetic.cpp src/guest_memory.cpp)
target_include_directories(sfr_integer_arithmetic_test PRIVATE src)
add_test(NAME integer_arithmetic COMMAND sfr_integer_arithmetic_test)
add_executable(sfr_store_halfword_update_test tests/store_halfword_update_test.cpp src/store_halfword_update.cpp src/guest_memory.cpp)
target_include_directories(sfr_store_halfword_update_test PRIVATE src)
add_test(NAME store_halfword_update COMMAND sfr_store_halfword_update_test)
add_executable(sfr_store_float_single_update_test tests/store_float_single_update_test.cpp src/store_float_single_update.cpp src/guest_memory.cpp)
target_include_directories(sfr_store_float_single_update_test PRIVATE src)
add_test(NAME store_float_single_update COMMAND sfr_store_float_single_update_test)
add_executable(sfr_load_halfword_update_test tests/load_halfword_update_test.cpp src/load_halfword_update.cpp src/guest_memory.cpp)
target_include_directories(sfr_load_halfword_update_test PRIVATE src)
add_test(NAME load_halfword_update COMMAND sfr_load_halfword_update_test)
add_executable(sfr_memory_update_forms_test tests/memory_update_forms_test.cpp src/memory_update_forms.cpp src/guest_memory.cpp)
target_include_directories(sfr_memory_update_forms_test PRIVATE src)
add_test(NAME memory_update_forms COMMAND sfr_memory_update_forms_test)
add_executable(sfr_touch_controls_test tests/touch_controls_test.cpp src/touch_controls.cpp)
target_include_directories(sfr_touch_controls_test PRIVATE src)
add_test(NAME touch_controls COMMAND sfr_touch_controls_test)
# The webcam standing in for the Kinect sensor: Media Foundation on Windows,
# nothing yet elsewhere (the setting then has no effect).
if(WIN32)
    set(SFR_CAMERA_CAPTURE src/camera_capture.cpp src/camera_capture_win32.cpp)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Linux")
    set(SFR_CAMERA_CAPTURE src/camera_capture.cpp src/camera_capture_v4l2.cpp)
else()
    set(SFR_CAMERA_CAPTURE src/camera_capture.cpp src/camera_capture_none.cpp)
endif()
add_executable(sfr_camera_capture_test tests/camera_capture_test.cpp src/camera_capture.cpp)
target_include_directories(sfr_camera_capture_test PRIVATE src)
add_test(NAME camera_capture COMMAND sfr_camera_capture_test)
# Checking a host's camera without starting the game (no test: it needs one).
add_executable(sfr_camera_probe tests/camera_probe.cpp ${SFR_CAMERA_CAPTURE})
target_include_directories(sfr_camera_probe PRIVATE src)
# Finding a body in a camera picture: ONNX Runtime and the pinned RTMPose
# model (tools/onnx, fetched by scripts/fetch_pose_model.py). Without them
# the camera can still give the title a picture, but not drive the player.
set(SFR_ONNX_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/tools/onnx/onnxruntime")
if(EXISTS "${SFR_ONNX_ROOT}/include/onnxruntime_cxx_api.h")
    set(SFR_POSE_AVAILABLE ON)
    add_library(sfr_onnxruntime INTERFACE)
    target_include_directories(sfr_onnxruntime INTERFACE "${SFR_ONNX_ROOT}/include")
    if(WIN32)
        target_link_libraries(sfr_onnxruntime INTERFACE "${SFR_ONNX_ROOT}/lib/onnxruntime.lib")
    else()
        target_link_libraries(sfr_onnxruntime INTERFACE "${SFR_ONNX_ROOT}/lib/libonnxruntime.so")
    endif()
    set(SFR_POSE_SOURCES src/pose_estimator.cpp src/pose_estimator_onnx.cpp)
else()
    set(SFR_POSE_AVAILABLE OFF)
    set(SFR_POSE_SOURCES src/pose_estimator.cpp src/pose_estimator_none.cpp)
endif()
add_executable(sfr_pose_estimator_test tests/pose_estimator_test.cpp src/pose_estimator.cpp)
target_include_directories(sfr_pose_estimator_test PRIVATE src)
add_test(NAME pose_estimator COMMAND sfr_pose_estimator_test)
add_executable(sfr_pose_skeleton_test tests/pose_skeleton_test.cpp src/pose_skeleton.cpp)
target_include_directories(sfr_pose_skeleton_test PRIVATE src)
add_test(NAME pose_skeleton COMMAND sfr_pose_skeleton_test)
# Steadying the points the model reads afresh from every picture.
add_executable(sfr_pose_smoothing_test tests/pose_smoothing_test.cpp src/pose_smoothing.cpp)
target_include_directories(sfr_pose_smoothing_test PRIVATE src)
add_test(NAME pose_smoothing COMMAND sfr_pose_smoothing_test)
if(SFR_POSE_AVAILABLE)
    # Running the model over a camera picture, or a BMP, without the game.
    add_executable(sfr_pose_probe tests/pose_probe.cpp ${SFR_POSE_SOURCES} ${SFR_CAMERA_CAPTURE})
    target_include_directories(sfr_pose_probe PRIVATE src)
    target_link_libraries(sfr_pose_probe PRIVATE sfr_onnxruntime)
endif()
# Which player each host controller is.
add_executable(sfr_pad_assignment_test tests/pad_assignment_test.cpp src/pad_assignment.cpp)
target_include_directories(sfr_pad_assignment_test PRIVATE src)
add_test(NAME pad_assignment COMMAND sfr_pad_assignment_test)
add_executable(sfr_native_input_test tests/native_input_test.cpp src/native_input.cpp src/pad_assignment.cpp src/sony_gamepad.cpp src/touch_controls.cpp src/guest_memory.cpp)
target_include_directories(sfr_native_input_test PRIVATE src)
if(WIN32)
    target_link_libraries(sfr_native_input_test PRIVATE xinput hid setupapi)
else()
    target_link_libraries(sfr_native_input_test PRIVATE SDL2::SDL2)
endif()
add_test(NAME native_input COMMAND sfr_native_input_test)
add_executable(sfr_native_audio_test tests/native_audio_test.cpp src/native_audio.cpp)
target_include_directories(sfr_native_audio_test PRIVATE src)
if(WIN32)
    target_link_libraries(sfr_native_audio_test PRIVATE xaudio2 ole32)
else()
    target_link_libraries(sfr_native_audio_test PRIVATE SDL2::SDL2)
endif()
add_test(NAME native_audio COMMAND sfr_native_audio_test)
add_executable(sfr_launcher_settings_test tests/launcher_settings_test.cpp src/launcher_settings.cpp)
target_include_directories(sfr_launcher_settings_test PRIVATE src)
add_test(NAME launcher_settings COMMAND sfr_launcher_settings_test)
add_executable(sfr_portable_waitables_test tests/portable_waitables_test.cpp src/portable_waitables.cpp)
target_include_directories(sfr_portable_waitables_test PRIVATE src)
add_test(NAME portable_waitables COMMAND sfr_portable_waitables_test)
add_executable(sfr_vulkan_shader_source_test tests/vulkan_shader_source_test.cpp src/vulkan_shader_source.cpp)
target_include_directories(sfr_vulkan_shader_source_test PRIVATE src)
add_test(NAME vulkan_shader_source COMMAND sfr_vulkan_shader_source_test)
add_executable(sfr_launcher_art_test tests/launcher_art_test.cpp src/launcher_art.cpp src/launcher_sound.cpp)
target_include_directories(sfr_launcher_art_test PRIVATE src)
if(WIN32)
    target_link_libraries(sfr_launcher_art_test PRIVATE winmm)
endif()
add_test(NAME launcher_art COMMAND sfr_launcher_art_test)
add_executable(sfr_installer_test tests/installer_test.cpp)
target_link_libraries(sfr_installer_test PRIVATE sfr_installer)
add_test(NAME installer COMMAND sfr_installer_test)
# The launcher (settings and install pages before the game), Dear ImGui:
# Direct3D 11 on Windows, SDL's renderer on Linux and Android.
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tools/imgui/imgui.cpp")
    set(SFR_IMGUI "${CMAKE_CURRENT_SOURCE_DIR}/tools/imgui")
    # The launcher lists the host's cameras for its camera setting.
    set(SFR_LAUNCHER_SOURCES src/launcher_main.cpp src/launcher_settings.cpp src/launcher_sound.cpp src/launcher_art.cpp src/sony_gamepad.cpp
        ${SFR_CAMERA_CAPTURE}
        ${SFR_IMGUI}/imgui.cpp ${SFR_IMGUI}/imgui_draw.cpp ${SFR_IMGUI}/imgui_tables.cpp ${SFR_IMGUI}/imgui_widgets.cpp)
    if(WIN32)
        add_executable(sfr_launcher WIN32 ${SFR_LAUNCHER_SOURCES} src/launcher_platform_win32.cpp
            ${SFR_IMGUI}/backends/imgui_impl_win32.cpp ${SFR_IMGUI}/backends/imgui_impl_dx11.cpp)
        target_link_libraries(sfr_launcher PRIVATE sfr_installer winmm imm32 d3d11 dxgi d3dcompiler hid setupapi ole32 shell32)
        set_target_properties(sfr_launcher PROPERTIES OUTPUT_NAME FreeRidersRecompiled)
    else()
        set(SFR_LAUNCHER_SOURCES ${SFR_LAUNCHER_SOURCES} src/launcher_platform_sdl.cpp
            ${SFR_IMGUI}/backends/imgui_impl_sdl2.cpp ${SFR_IMGUI}/backends/imgui_impl_sdlrenderer2.cpp)
        if(ANDROID)
            # liblauncher.so, run by LauncherActivity (android/).
            add_library(sfr_launcher SHARED ${SFR_LAUNCHER_SOURCES})
            set_target_properties(sfr_launcher PROPERTIES OUTPUT_NAME launcher)
            target_link_libraries(sfr_launcher PRIVATE android log)
        else()
            add_executable(sfr_launcher ${SFR_LAUNCHER_SOURCES})
            set_target_properties(sfr_launcher PROPERTIES OUTPUT_NAME FreeRidersRecompiled)
        endif()
        target_compile_definitions(sfr_launcher PRIVATE SFR_UI_SOUND_SDL)
        target_link_libraries(sfr_launcher PRIVATE sfr_installer SDL2::SDL2)
    endif()
    target_include_directories(sfr_launcher PRIVATE src ${SFR_IMGUI} ${SFR_IMGUI}/backends)
endif()
add_executable(sfr_texture_fetch_test tests/texture_fetch_test.cpp src/texture_fetch.cpp)
target_include_directories(sfr_texture_fetch_test PRIVATE src)
add_test(NAME texture_fetch COMMAND sfr_texture_fetch_test)
add_executable(sfr_vector_integer_test tests/vector_integer_test.cpp)
target_include_directories(sfr_vector_integer_test PRIVATE src)
add_test(NAME vector_integer COMMAND sfr_vector_integer_test)
add_executable(sfr_memory_statistics_test tests/memory_statistics_test.cpp src/memory_statistics.cpp src/virtual_memory.cpp src/physical_memory.cpp src/guest_memory.cpp)
target_include_directories(sfr_memory_statistics_test PRIVATE src)
add_test(NAME memory_statistics COMMAND sfr_memory_statistics_test)
add_executable(sfr_video_mode_test tests/video_mode_test.cpp src/video_mode.cpp src/guest_memory.cpp)
target_include_directories(sfr_video_mode_test PRIVATE src)
add_test(NAME video_mode COMMAND sfr_video_mode_test)
add_executable(sfr_physical_memory_test tests/physical_memory_test.cpp src/physical_memory.cpp src/guest_memory.cpp)
target_include_directories(sfr_physical_memory_test PRIVATE src)
add_test(NAME physical_memory COMMAND sfr_physical_memory_test)
add_executable(sfr_resource_coherency_test tests/resource_coherency_test.cpp src/resource_coherency.cpp src/physical_memory.cpp src/guest_memory.cpp)
target_include_directories(sfr_resource_coherency_test PRIVATE src)
add_test(NAME resource_coherency COMMAND sfr_resource_coherency_test)
add_executable(sfr_ansi_string_test tests/ansi_string_test.cpp src/ansi_string.cpp src/guest_memory.cpp)
target_include_directories(sfr_ansi_string_test PRIVATE src)
add_test(NAME ansi_string COMMAND sfr_ansi_string_test)
if(WIN32 OR UNIX)
    add_library(sfr_native_thread STATIC ${SFR_NATIVE_THREAD})
    target_include_directories(sfr_native_thread PUBLIC src)
    if(WIN32)  # checks Windows kernel/file behaviour directly
        add_executable(sfr_native_thread_test tests/native_thread_test.cpp src/guest_memory.cpp)
        target_link_libraries(sfr_native_thread_test PRIVATE sfr_native_thread)
        add_test(NAME native_thread COMMAND sfr_native_thread_test)
        set_tests_properties(native_thread PROPERTIES TIMEOUT 10)
    endif()
    add_library(sfr_guest_threads STATIC src/guest_threads.cpp)
    target_link_libraries(sfr_guest_threads PUBLIC sfr_native_thread)
    add_executable(sfr_guest_threads_test tests/guest_threads_test.cpp src/guest_memory.cpp src/system_time.cpp)
    target_link_libraries(sfr_guest_threads_test PRIVATE sfr_guest_threads)
    add_test(NAME guest_threads COMMAND sfr_guest_threads_test)
    set_tests_properties(guest_threads PROPERTIES TIMEOUT 10)
    add_library(sfr_native_sync_objects STATIC src/native_sync_objects.cpp ${SFR_PORTABLE_WAITABLES} src/guest_sync_objects.cpp)
    target_include_directories(sfr_native_sync_objects PUBLIC src)
    add_executable(sfr_native_sync_objects_test tests/native_sync_objects_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_native_sync_objects_test PRIVATE sfr_native_sync_objects)
    add_test(NAME native_sync_objects COMMAND sfr_native_sync_objects_test)
    set_tests_properties(native_sync_objects PROPERTIES TIMEOUT 10)
    add_executable(sfr_native_notification_event_test tests/native_notification_event_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_native_notification_event_test PRIVATE sfr_native_sync_objects)
    add_test(NAME native_notification_event COMMAND sfr_native_notification_event_test)
    add_executable(sfr_notification_placement_test tests/notification_placement_test.cpp src/guest_memory.cpp)
    target_include_directories(sfr_notification_placement_test PRIVATE src)
    add_test(NAME notification_placement COMMAND sfr_notification_placement_test)
    add_library(sfr_native_notifications STATIC src/native_notifications.cpp)
    target_link_libraries(sfr_native_notifications PUBLIC sfr_native_sync_objects)
    add_executable(sfr_native_notifications_test tests/native_notifications_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_native_notifications_test PRIVATE sfr_native_notifications)
    add_test(NAME native_notifications COMMAND sfr_native_notifications_test)
    set_tests_properties(native_notifications PROPERTIES TIMEOUT 10)
    add_executable(sfr_guest_sync_objects_test tests/guest_sync_objects_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_sync_objects_test PRIVATE sfr_native_sync_objects)
    add_test(NAME guest_sync_objects COMMAND sfr_guest_sync_objects_test)
    if(WIN32)  # checks Windows kernel/file behaviour directly
        add_executable(sfr_native_named_sync_test tests/native_named_sync_test.cpp src/guest_memory.cpp)
        target_link_libraries(sfr_native_named_sync_test PRIVATE sfr_native_sync_objects)
        add_test(NAME native_named_sync COMMAND sfr_native_named_sync_test)
        set_tests_properties(native_named_sync PROPERTIES TIMEOUT 10)
    endif()
    add_executable(sfr_guest_named_sync_test tests/guest_named_sync_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_named_sync_test PRIVATE sfr_native_sync_objects)
    add_test(NAME guest_named_sync COMMAND sfr_guest_named_sync_test)
    set_tests_properties(guest_named_sync PROPERTIES TIMEOUT 10)
    target_sources(sfr_native_sync_objects PRIVATE src/guest_wait.cpp)
    add_executable(sfr_guest_wait_test tests/guest_wait_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_wait_test PRIVATE sfr_native_sync_objects)
    add_test(NAME guest_wait COMMAND sfr_guest_wait_test)
    set_tests_properties(guest_wait PROPERTIES TIMEOUT 10)
    add_library(sfr_asset_files STATIC ${SFR_ASSET_FILES} src/guest_files.cpp)
    target_include_directories(sfr_asset_files PUBLIC src)
    if(WIN32)  # checks Windows kernel/file behaviour directly
        add_executable(sfr_asset_files_test tests/asset_files_test.cpp)
        target_link_libraries(sfr_asset_files_test PRIVATE sfr_asset_files)
        target_include_directories(sfr_asset_files_test PRIVATE src)
        add_test(NAME asset_files COMMAND sfr_asset_files_test)
    endif()
    if(WIN32)  # checks Windows kernel/file behaviour directly
        add_executable(sfr_async_asset_files_test tests/async_asset_files_test.cpp)
        target_link_libraries(sfr_async_asset_files_test PRIVATE sfr_asset_files)
        add_test(NAME async_asset_files COMMAND sfr_async_asset_files_test)
        set_tests_properties(async_asset_files PROPERTIES TIMEOUT 20)
    endif()
    if(WIN32)  # checks Windows kernel/file behaviour directly
        add_executable(sfr_native_async_read_test tests/native_async_read_test.cpp)
        target_link_libraries(sfr_native_async_read_test PRIVATE sfr_asset_files)
        add_test(NAME native_async_read COMMAND sfr_native_async_read_test)
        set_tests_properties(native_async_read PROPERTIES TIMEOUT 30)
    endif()
    add_executable(sfr_async_completion_primitives_test tests/async_completion_primitives_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_async_completion_primitives_test PRIVATE sfr_guest_execution sfr_native_sync_objects)
    add_test(NAME async_completion_primitives COMMAND sfr_async_completion_primitives_test)
    set_tests_properties(async_completion_primitives PROPERTIES TIMEOUT 20)
    add_library(sfr_guest_async_files STATIC src/guest_async_files.cpp)
    target_link_libraries(sfr_guest_async_files PUBLIC sfr_asset_files sfr_native_sync_objects sfr_guest_execution)
    add_executable(sfr_guest_async_files_test tests/guest_async_files_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_async_files_test PRIVATE sfr_guest_async_files)
    add_test(NAME guest_async_files COMMAND sfr_guest_async_files_test)
    set_tests_properties(guest_async_files PROPERTIES TIMEOUT 20)
    add_executable(sfr_guest_files_test tests/guest_files_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_files_test PRIVATE sfr_asset_files)
    target_include_directories(sfr_guest_files_test PRIVATE src)
    add_test(NAME guest_files COMMAND sfr_guest_files_test)
    add_executable(sfr_guest_file_read_test tests/guest_file_read_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_file_read_test PRIVATE sfr_asset_files)
    add_test(NAME guest_file_read COMMAND sfr_guest_file_read_test)
    if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tools/Plume/CMakeLists.txt")
        message(FATAL_ERROR "Run scripts/bootstrap.py to obtain the pinned Plume renderer.")
    endif()
    set(PLUME_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
    set(D3D12_AGILITY_SDK_ENABLED OFF CACHE BOOL "" FORCE)
    # Plume draws into an SDL window, except on Android, where it draws into
    # the window's ANativeWindow (RenderWindow is one there). Its window-size
    # query prefers the SDL branch whenever this is on, so with it on for
    # Android it read the ANativeWindow as an SDL_Window: every swap chain was
    # made (uint32)-19 wide, and the blit's viewport and scissor with it.
    if(NOT WIN32 AND NOT ANDROID)
        set(SDL_VULKAN_ENABLED ON CACHE BOOL "" FORCE)
    endif()
    add_subdirectory(tools/Plume)
    if(APPLE)
        # Plume's Apple glue asks IOKit for the GPU vendor and uses Metal and
        # Cocoa objects; its own CMake leaves linking them to the user.
        target_link_libraries(plume PUBLIC "-framework CoreFoundation" "-framework IOKit" "-framework Foundation"
            "-framework Metal" "-framework QuartzCore" "-framework AppKit" objc)
    endif()
    if(NOT WIN32 AND SDL2_VERSION VERSION_LESS 2.26)
        # Plume asks SDL for the drawable size by the name SDL 2.26 gave it;
        # older SDL2 has the same call as SDL_Vulkan_GetDrawableSize.
        target_compile_definitions(plume PRIVATE SDL_GetWindowSizeInPixels=SDL_Vulkan_GetDrawableSize)
    endif()
    # -DSFR_VULKAN_VALIDATION=ON asks Plume for the Khronos validation layer,
    # for diagnosis only: the layer has to be beside the program (or in the
    # APK's jniLibs on Android) and it costs a great deal of speed.
    option(SFR_VULKAN_VALIDATION "Enable the Vulkan validation layer" OFF)
    if(SFR_VULKAN_VALIDATION)
        target_compile_definitions(plume PRIVATE VULKAN_VALIDATION_LAYER_ENABLED)
    endif()
    add_library(sfr_native_graphics STATIC src/native_graphics.cpp)
    target_include_directories(sfr_native_graphics PUBLIC src)
    if(WIN32)
        target_link_libraries(sfr_native_graphics PUBLIC plume PRIVATE d3d12 dxgi)
    else()
        target_link_libraries(sfr_native_graphics PUBLIC plume SDL2::SDL2)
    endif()
    set(SFR_SHADER_CACHE_SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/out/shaders/basic/shader_cache_data.cpp" CACHE FILEPATH "Generated private shader cache")
    if(EXISTS "${SFR_SHADER_CACHE_SOURCE}")
        set(SFR_SHADER_CACHE_DATA "${SFR_SHADER_CACHE_SOURCE}")
    else()
        set(SFR_SHADER_CACHE_DATA src/shader_cache_empty.cpp)
    endif()
    add_library(sfr_native_shaders STATIC src/native_shaders.cpp src/runtime_shader_cache.cpp src/vulkan_shader_source.cpp src/vertex_palette.cpp src/loop_constants.cpp src/shader_inputs.cpp ${SFR_SHADER_CACHE_DATA})
    target_link_libraries(sfr_native_shaders PUBLIC sfr_native_graphics)
    add_executable(sfr_native_shaders_test tests/native_shaders_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_native_shaders_test PRIVATE sfr_native_shaders)
    add_test(NAME native_shaders COMMAND sfr_native_shaders_test)
    set_tests_properties(native_shaders PROPERTIES TIMEOUT 20)
    add_executable(sfr_native_graphics_test tests/native_graphics_test.cpp)
    target_link_libraries(sfr_native_graphics_test PRIVATE sfr_native_graphics)
    add_test(NAME native_graphics COMMAND sfr_native_graphics_test)
    set_tests_properties(native_graphics PROPERTIES TIMEOUT 20)
    add_library(sfr_native_raster_state STATIC src/native_raster_state.cpp)
    target_link_libraries(sfr_native_raster_state PUBLIC sfr_native_graphics)
    # The backend's own shaders, compiled at build time to DXIL and SPIR-V with
    # XenosRecomp's pinned dxc-bin and embedded as byte arrays. The compiler
    # runs on the build machine (an Android build uses the host's).
    if(CMAKE_HOST_WIN32)
        set(SFR_DXC "${CMAKE_CURRENT_SOURCE_DIR}/tools/XenosRecomp/thirdparty/dxc-bin/bin/x64/dxc.exe")
    elseif(CMAKE_HOST_APPLE)
        # dxc-macos (universal) loads libdxcompiler.dylib from dxc-bin's lib directory.
        set(SFR_DXC ${CMAKE_COMMAND} -E env "DYLD_LIBRARY_PATH=${CMAKE_CURRENT_SOURCE_DIR}/tools/XenosRecomp/thirdparty/dxc-bin/lib/x64"
            "${CMAKE_CURRENT_SOURCE_DIR}/tools/XenosRecomp/thirdparty/dxc-bin/bin/x64/dxc-macos")
    else()
        # dxc-linux loads libdxcompiler.so from dxc-bin's lib directory.
        set(SFR_DXC ${CMAKE_COMMAND} -E env "LD_LIBRARY_PATH=${CMAKE_CURRENT_SOURCE_DIR}/tools/XenosRecomp/thirdparty/dxc-bin/lib/x64"
            "${CMAKE_CURRENT_SOURCE_DIR}/tools/XenosRecomp/thirdparty/dxc-bin/bin/x64/dxc-linux")
    endif()
    if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/tools/XenosRecomp/thirdparty/dxc-bin/bin/x64")
        message(FATAL_ERROR "Run scripts/bootstrap.py to obtain XenosRecomp's dxc-bin.")
    endif()
    set(SFR_EMBEDDED_SHADERS "${CMAKE_CURRENT_BINARY_DIR}/embedded_shaders")
    file(MAKE_DIRECTORY "${SFR_EMBEDDED_SHADERS}")
    set(SFR_EMBEDDED_SHADER_HEADERS)
    function(sfr_embed_shader source entry profile name)
        set(header "${SFR_EMBEDDED_SHADERS}/${name}_dxil.h")
        add_custom_command(OUTPUT "${header}"
            COMMAND ${SFR_DXC} -T ${profile} -E ${entry} -HV 2021 -Qstrip_debug -Qstrip_reflect -Fh "${header}" -Vn ${name}_dxil "${CMAKE_CURRENT_SOURCE_DIR}/${source}"
            DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/${source}" VERBATIM)
        set(spirv_header "${SFR_EMBEDDED_SHADERS}/${name}_spirv.h")
        set(invert)
        if(profile MATCHES "^vs_")
            set(invert -fvk-invert-y)  # D3D clip space in, Vulkan's out
        endif()
        add_custom_command(OUTPUT "${spirv_header}"
            COMMAND ${SFR_DXC} -T ${profile} -E ${entry} -HV 2021 -spirv -fvk-use-dx-layout ${invert} -Qstrip_debug -Fh "${spirv_header}" -Vn ${name}_spirv "${CMAKE_CURRENT_SOURCE_DIR}/${source}"
            DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/${source}" VERBATIM)
        set(SFR_EMBEDDED_SHADER_HEADERS ${SFR_EMBEDDED_SHADER_HEADERS} "${header}" "${spirv_header}" PARENT_SCOPE)
    endfunction()
    sfr_embed_shader(src/shaders/blit.hlsl vertexMain vs_6_0 blit_vs)
    sfr_embed_shader(src/shaders/blit.hlsl pixelMain ps_6_0 blit_ps)
    add_library(sfr_native_presentation STATIC src/native_presentation.cpp src/touch_controls.cpp ${SFR_EMBEDDED_SHADER_HEADERS})
    target_include_directories(sfr_native_presentation PRIVATE "${SFR_EMBEDDED_SHADERS}")
    add_executable(sfr_native_presentation_test tests/native_presentation_test.cpp)
    if(WIN32)
        target_link_libraries(sfr_native_presentation PUBLIC sfr_native_raster_state PRIVATE user32)
        target_link_libraries(sfr_native_presentation_test PRIVATE sfr_native_presentation user32)
    else()
        target_link_libraries(sfr_native_presentation PUBLIC sfr_native_raster_state PRIVATE SDL2::SDL2)
        target_link_libraries(sfr_native_presentation_test PRIVATE sfr_native_presentation)
    endif()
    add_test(NAME native_presentation COMMAND sfr_native_presentation_test)
    if(WIN32)  # drives D3D12 directly
    add_executable(sfr_native_raster_state_test tests/native_raster_state_test.cpp)
    target_link_libraries(sfr_native_raster_state_test PRIVATE sfr_native_presentation d3dcompiler)
    add_test(NAME native_raster_state COMMAND sfr_native_raster_state_test)
    set_tests_properties(native_raster_state PROPERTIES TIMEOUT 20)
    endif()
    add_library(sfr_native_blend_control STATIC src/native_blend_control.cpp)
    target_include_directories(sfr_native_blend_control PUBLIC src)
    target_link_libraries(sfr_native_blend_control PUBLIC plume)
    add_executable(sfr_native_blend_control_test tests/native_blend_control_test.cpp)
    target_link_libraries(sfr_native_blend_control_test PRIVATE sfr_native_blend_control)
    add_test(NAME native_blend_control COMMAND sfr_native_blend_control_test)
    add_library(sfr_guest_graphics STATIC src/guest_graphics.cpp src/guest_graphics_state.cpp src/texture_fetch.cpp src/native_formats.cpp src/native_renderer.cpp)
    if(MSVC)
        target_compile_options(sfr_guest_graphics PRIVATE /fp:strict)
    elseif(CMAKE_CXX_COMPILER_ID MATCHES "Clang")
        target_compile_options(sfr_guest_graphics PRIVATE -ffp-model=strict)
    else()
        target_compile_options(sfr_guest_graphics PRIVATE -frounding-math -ffp-contract=off)
    endif()
    target_link_libraries(sfr_guest_graphics PUBLIC sfr_native_presentation sfr_native_shaders sfr_native_blend_control)
    add_executable(sfr_guest_graphics_test tests/guest_graphics_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_graphics_test PRIVATE sfr_guest_graphics)
    add_test(NAME guest_graphics COMMAND sfr_guest_graphics_test)
    add_executable(sfr_native_formats_test tests/native_formats_test.cpp)
    target_link_libraries(sfr_native_formats_test PRIVATE sfr_guest_graphics)
    add_test(NAME native_formats COMMAND sfr_native_formats_test)
    set_tests_properties(native_presentation guest_graphics PROPERTIES TIMEOUT 20)
    add_executable(sfr_guest_render_state_test tests/guest_render_state_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_render_state_test PRIVATE sfr_guest_graphics)
    add_test(NAME guest_render_state COMMAND sfr_guest_render_state_test)
    set_tests_properties(guest_render_state PROPERTIES TIMEOUT 20)
    add_executable(sfr_guest_blend_request_test tests/guest_blend_request_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_blend_request_test PRIVATE sfr_guest_graphics)
    add_test(NAME guest_blend_request COMMAND sfr_guest_blend_request_test)
    set_tests_properties(guest_blend_request PROPERTIES TIMEOUT 20)
    add_executable(sfr_guest_sampler_filter_test tests/guest_sampler_filter_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_sampler_filter_test PRIVATE sfr_guest_graphics)
    add_test(NAME guest_sampler_filter COMMAND sfr_guest_sampler_filter_test)
    set_tests_properties(guest_sampler_filter PROPERTIES TIMEOUT 20)
    add_executable(sfr_guest_texture_binding_test tests/guest_texture_binding_test.cpp src/guest_memory.cpp)
    target_link_libraries(sfr_guest_texture_binding_test PRIVATE sfr_guest_graphics)
    add_test(NAME guest_texture_binding COMMAND sfr_guest_texture_binding_test)
    set_tests_properties(guest_texture_binding PROPERTIES TIMEOUT 20)
endif()
if(SFR_BUILD_DIAGNOSTIC)
    if(WIN32)  # compared with the host's own Winsock negotiation
        add_executable(sfr_native_winsock_test tests/native_winsock_test.cpp src/native_winsock.cpp src/guest_memory.cpp)
        target_include_directories(sfr_native_winsock_test PRIVATE src)
        target_link_libraries(sfr_native_winsock_test PRIVATE ws2_32)
        add_test(NAME native_winsock COMMAND sfr_native_winsock_test)
    endif()
    set(SFR_DIAGNOSTIC_DIR "${CMAKE_CURRENT_SOURCE_DIR}/out/recomp/diagnostic" CACHE PATH "Audited generated diagnostic sources")
    if(NOT EXISTS "${SFR_DIAGNOSTIC_DIR}/report.json")
        message(FATAL_ERROR "Run scripts/generate_diagnostic.py first; raw output is not safe to execute.")
    endif()
    file(GLOB SFR_DIAGNOSTIC_SOURCES CONFIGURE_DEPENDS "${SFR_DIAGNOSTIC_DIR}/ppc_recomp.*.cpp")
    if(ANDROID)
        # The game is libmain.so; SDL's activity calls its SDL_main
        # (src/android_main.cpp), which sets up and calls the usual main.
        add_library(sfr_cpu_diagnostic SHARED src/android_main.cpp)
        set_target_properties(sfr_cpu_diagnostic PROPERTIES OUTPUT_NAME main)
        set_source_files_properties(src/diagnostic_main.cpp PROPERTIES COMPILE_DEFINITIONS main=sfr_game_main)
        target_link_libraries(sfr_cpu_diagnostic PRIVATE android log)
        # The NDK hardens every translation unit by default. The guest code is
        # thirty thousand generated functions with large frames that only ever
        # touch the checked guest memory, so the canaries and the fortified
        # library calls buy nothing here and are paid for in every one of them.
        target_compile_options(sfr_cpu_diagnostic PRIVATE -fno-stack-protector -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0)
    else()
        add_executable(sfr_cpu_diagnostic)
    endif()
    target_sources(sfr_cpu_diagnostic PRIVATE src/diagnostic_main.cpp src/guest_memory.cpp src/xex_module.cpp src/virtual_memory.cpp src/critical_section.cpp src/hardware_info.cpp src/thread_local_storage.cpp src/system_time.cpp src/guest_clock.cpp src/timestamp_bundle.cpp src/vector_memory.cpp src/optional_import_policy.cpp src/native_modules.cpp src/integer_arithmetic.cpp src/memory_statistics.cpp
        ${SFR_DIAGNOSTIC_SOURCES} "${SFR_DIAGNOSTIC_DIR}/ppc_func_mapping.cpp" "${SFR_DIAGNOSTIC_DIR}/imports.cpp")
    target_include_directories(sfr_cpu_diagnostic PRIVATE src "${SFR_DIAGNOSTIC_DIR}" tools/XenonRecomp/thirdparty/simde tools/XenonRecomp/thirdparty/TinySHA1)
    target_sources(sfr_cpu_diagnostic PRIVATE src/video_mode.cpp src/native_display.cpp src/physical_memory.cpp src/ansi_string.cpp src/store_halfword_update.cpp src/store_float_single_update.cpp src/load_halfword_update.cpp src/debug_monitor.cpp src/image_protection.cpp src/resource_coherency.cpp src/video_globals.cpp src/native_language.cpp src/system_config.cpp)
    target_sources(sfr_cpu_diagnostic PRIVATE src/game_region.cpp)
    # The webcam standing in for the sensor, and the body it finds.
    target_sources(sfr_cpu_diagnostic PRIVATE src/camera_player.cpp src/pose_skeleton.cpp src/pose_smoothing.cpp
                   ${SFR_CAMERA_CAPTURE} ${SFR_POSE_SOURCES})
    if(SFR_POSE_AVAILABLE)
        target_link_libraries(sfr_cpu_diagnostic PRIVATE sfr_onnxruntime)
    endif()
    target_sources(sfr_cpu_diagnostic PRIVATE src/memory_update_forms.cpp)
    target_sources(sfr_cpu_diagnostic PRIVATE src/native_input.cpp src/pad_assignment.cpp src/sony_gamepad.cpp src/native_audio.cpp src/nui_skeleton.cpp src/nui_speech.cpp src/nui_race.cpp)
    target_sources(sfr_cpu_diagnostic PRIVATE src/guest_printf.cpp)
    if(WIN32)
        target_link_libraries(sfr_cpu_diagnostic PRIVATE xinput hid setupapi xaudio2 ole32)
        # Symbol map for host-side profiling (SFR_HOST_PROFILE).
        target_link_options(sfr_cpu_diagnostic PRIVATE "/MAP:$<TARGET_FILE_DIR:sfr_cpu_diagnostic>/sfr_cpu_diagnostic.map")
    endif()
    target_sources(sfr_cpu_diagnostic PRIVATE src/native_country.cpp)
    target_sources(sfr_cpu_diagnostic PRIVATE src/nui_device_status.cpp)
    target_sources(sfr_cpu_diagnostic PRIVATE src/unselected_users.cpp src/local_profile.cpp src/content_files.cpp)
    target_sources(sfr_cpu_diagnostic PRIVATE src/multibyte_unicode.cpp)
    target_sources(sfr_cpu_diagnostic PRIVATE src/guest_critical_sections.cpp)
    target_link_libraries(sfr_cpu_diagnostic PRIVATE sfr_guest_execution)
    target_sources(sfr_cpu_diagnostic PRIVATE src/guest_graphics_hooks.cpp src/game_patches.cpp src/nui_hooks.cpp src/nui_race_hooks.cpp)
    target_sources(sfr_cpu_diagnostic PRIVATE src/native_winsock.cpp)
    target_link_libraries(sfr_cpu_diagnostic PRIVATE sfr_native_notifications)
    target_link_libraries(sfr_cpu_diagnostic PRIVATE sfr_guest_graphics sfr_asset_files sfr_native_sync_objects sfr_guest_threads)
    target_link_libraries(sfr_cpu_diagnostic PRIVATE sfr_guest_async_files)
    if(WIN32)
        target_link_libraries(sfr_cpu_diagnostic PRIVATE ws2_32 user32)
    else()
        target_link_libraries(sfr_cpu_diagnostic PRIVATE SDL2::SDL2)
    endif()
    if(NOT CMAKE_CXX_COMPILER_ID STREQUAL "Clang")
        message(FATAL_ERROR "The generated diagnostic currently requires Clang.")
    endif()
    target_compile_options(sfr_cpu_diagnostic PRIVATE -fno-strict-aliasing -Wno-unused-variable -Wno-unused-value)
    if(MSVC)
        target_compile_options(sfr_cpu_diagnostic PRIVATE "/FI${CMAKE_CURRENT_SOURCE_DIR}/src/diagnostic_hooks.h" /fp:strict)
    else()
        target_compile_options(sfr_cpu_diagnostic PRIVATE -include "${CMAKE_CURRENT_SOURCE_DIR}/src/diagnostic_hooks.h" -ffp-model=strict)
    endif()
    if(CMAKE_SYSTEM_PROCESSOR MATCHES "AMD64|amd64|x86_64")
        target_compile_options(sfr_cpu_diagnostic PRIVATE -march=sandybridge)
    endif()
endif()
