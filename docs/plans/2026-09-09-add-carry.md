# Plain addc in the original texture transfer

ae1e044 completes original DDS parsing/copy and reaches8250C848. Two missing
plain addc instructions at8250CA9C/8250CAAC feed subfe-generated masks used by
the original texture transfer. Auditout/next-addc-audit.json verifies all46plain
sites in28functions against originalwords and exclusivelymatchinglogevents.

Pinned Xenia independently specifies full64-bit modulo addition and carry from
the low32-bit operands for this Xbox profile. IncomingCA is ignored/overwritten;
plainform leavesCR,overflow andstickyoverflow unchanged. r0 is anordinarysource.
Usepass-by-value operands and reference destination/carry so aliases are correct.
Do notcopy subfze'soldcarryvalidation into anoperationthatdoesnotconsumeoldcarry.

Strictgeneratorrewrite accepts only canonicalempty plainaddc blocks with
exclusivelymatchingunsupportedlogevents. Rejectvariants,noncanonicaloperands,
preexistingimplementations andextra statementsincludingthosehiddenafterlabels.
Preservecomments/labels/checkpoints andunrelateddiagnostics. Recordretainedsites.

FirstverifyhelperandgeneratorRED, implementGREEN, freshgenerateandcompare all
input/loghashes andretentiondelta. Build/runoriginalentry tothenextdependency;
preserveDDSbytecomparisonandparserownershipproof. Independentspec/qualityreview,
fullnative/Pythontestsandlocalcommit follow. Originaltitle/menu remainsunachieved.

The addc build now enters the original transfer and reaches824F29E8, whose two
plain addme sites824F2C90/824F2CE0 block layout calculation. Extend this checkpoint
to that immediate dependency. Both words are7D4A01D4; the independent audit covers
51 annotations,50 distinct sites and36 functions (out/next-addme-audit.json).
Implement full64 result RA+oldCA-1 and low32 carry oldCA || RA32!=0. Validate
incoming carry0/1 before mutation. Use the same exact logged empty-block policy,
but two register operands. Run native and generator RED/GREEN before fresh
generation, original-entry execution and final combined tests/review/commit.
The current source format has bit100 clear and skips the two addc instructions
inside8250C848; retaining that function permits entry, but does not prove those
two sites executed. Keep this distinction in the recorded runtime evidence.
