# 用手把進行比賽（進行中）

日期：2026-09-20。分支 `claude/function-boundaries`。接續 [用手把操作選單](pad-menus.md)。

比賽中的操作已經實作，但**還沒有實際跑完一場比賽**：比賽場景的畫面需要離屏算繪目標
（render to texture），原生後端還沒有支援（見最後一節）。目前教學關可以載入、跳過
「On your Gear!」準備動作、進入比賽場景，然後停在該處。

## 手把對應

| 操作 | 按鍵 |
| --- | --- |
| 蹲下蓄力 / 跳躍 | 按住 A／放開 A |
| 踢地加速（Kick Dash） | 按 X 再放開 |
| 煞車、抓取（桿子、軌道） | 按住 B |
| 傾斜轉彎、空中特技旋轉 | 左類比 |
| 切換站姿（Regular／Goofy） | Y |
| 左右招式（Power Skill） | LB／RB |
| 使用道具（按）、搖晃道具（按住）、擦螢幕（按住＋右類比） | RT |
| 舉手 | 右類比上（雙手） |
| 往左／右伸手（拿旁邊的 Ring） | 右類比左／右（推越多伸越遠） |

鍵盤：方向鍵是左類比，Z／空白 A，X B，C X，V Y，Q／E LB／RB，**F RT**，**R LT**，
I／J／K／L 右類比。PS5（DualSense）與 PS4 手把透過 HID 直接讀取（見 `src/sony_gamepad.cpp`）。

**傾斜**（2026-09-21 修正）：遊戲在 `822C6200` 等處比較身體紀錄的 +640 與 +644：
640 較大往右傾 640/644−1，644 較大往左傾 644/640−1，最多 3.5，相等時直立；**+640 為 0
時直接當成傾斜 1.0**。之前只寫 +644（中立 0.5），+640 一直是 0，所以不操作也會右傾。
現在兩個都寫：中立時都是 1，搖桿推到底時一邊是 1+`SFR_RACE_LEAN_SCALE`（預設 1）。

## 做法

比賽中遊戲讀的是每位玩家的「身體狀態」（KinnectNuiBox+0x78 的 4788 位元組紀錄，
原本由骨架資料填入），再交給各種手勢判斷器（vtable 在 821A22xx 一帶，
`detect(判斷器, 來源, 結果)`）。所以：

- **身體狀態**（[nui_race.cpp](../src/nui_race.cpp)）：比賽中（`[83E52F8C]` 非零）
  在 Kinect 管理器每格更新（82438930）之前，把 P1 的紀錄換成我們自己的一份，
  先複製遊戲原本的內容（指標等欄位要保持有效），再寫入手把對應的欄位：
  +8 按住 A、+4／+36 放開 A、+48／+52 X 按下／放開、+72 Y、+88／+32／+644 左右傾斜、
  +40 左類比上下、+16／+96 右類比、+92 RT、+256 LT、+260／+264 LB／RB、+268／+272 B、
  +740／+756 雙手追蹤狀態。離開比賽時把原本的紀錄換回去。
- **手勢判斷器**（[nui_race_hooks.cpp](../src/nui_race_hooks.cpp)）：改寫約 25 個
  判斷器，直接看手把決定結果，並寫入遊戲原本的結果旗標（結果紀錄每筆 84 位元組，
  +4 與 +20 收集旗標），例如跳躍 0x200、蹲下 0x7000、踢地 0x00C00000 → 0x01400000、
  煞車 0x00400100、招式 0x80000／0x100000、站姿 0x200000、特技旋轉 0x800（+56 累積圈數）。
- **賽前準備**：比賽前的「On your Gear!」（狀態在 +2408：0 開始、1 等待、2..6 量測身體、
  7 結束、8 開跑）會要求一連串只有真人身體做得到的姿勢，手把沒有身體可量，
  所以用手把進行時直接跳過 2..6。

## 為了載入比賽而補上的東西

- 原生裝置 Reset（824F4858）：比賽會用自己的後緩衝大小（880 寬）、多重取樣與垂直同步
  重設裝置。原生裝置保留自己的交換鏈，只更新遊戲讀得到的參數。
- 裝置狀態全部解除綁定（824F49C0）與 SetRenderTarget(1..3, NULL)、
  其他參照仍在時的 Device::Release（824F4800）。
- 資源釋放：型別 1..9 且沒有 GPU fence 的資源可以讀取裝置全域（它只在等待 fence 時會用到），
  以及 `MmFreePhysicalMemory`（GPU 或 write-combined 記憶體只忘記、不解除對應）。
- Kinect 深度影像（DepthView）：工作執行緒在主執行緒建立物件之前就會使用它，
  沒有物件時跳過該格。

## 教學關的分段選單

每一段課程結束後會出現環狀選單（Next／Retry／設定…），疊在比賽場景上（所以比賽旗標
仍是開著的）。推進它的是 **A（`ok`）**：選取目前對焦的環狀項目。連續按 A 可以一課一課
跑下去，但盲按會把環轉到「Retry」而重跑同一課，要穩定推進得先用十字鍵把焦點移到
「Next」。

`next` 這個字也送得出去（手把 **X**，比賽中也送，因為這些選單疊在比賽上），但這一頁
不認它——加它是為了其他有 Next 按鈕的頁面。

## 還沒完成

- **離屏算繪目標**：比賽場景會把畫面畫到自己的色彩與深度表面再取用，原生後端目前只有
  一組框架緩衝，`SFR_ALLOW_RENDER_TARGETS=1` 把它們全部別名到同一組，靠每個 pass 之後
  的解析複本串起來。比賽畫面已經完整顯示（見
  [比賽畫面](race-scene-rendering.md) 與 [骨骼調色盤與迴圈常數](shader-vertex-palette.md)），
  但解析複本一律是整個框架緩衝大小、深度解析沒有複製，真正的離屏目標仍然要做。
- 一般比賽（Free Race 等）在**裝備零件畫面**卡住：兩個插槽放滿後，換頁的延遲動作被
  82463808 擋住（它認為零件還在飛向板子）。教學關不經過這個畫面。
- 比賽中的判斷器都還沒有在真正的比賽裡驗證過。

## 2026-09-25：第二位玩家的身體在哪，以及怎麼在無人看管的執行中進到選單

比賽的操作和選單走的是兩條不同的路。選單是手部游標加上注入的語音詞
（[pad-menus.md](pad-menus.md)），比賽則是**每位玩家自己的 body record**：
`nui_race_hooks` 把第一位玩家的 record（`KinnectNuiBox+0x78`）換成我們從手把
填的那一份，約 25 個手勢偵測器再從它回答。

### 第二位玩家的 record 不在 Kinect manager 裡

把 manager 整整 8208 bytes 掃過一遍，找所有落在 record 同一個 64 KB 區段的
指標，結果只有 `+0x78` 一個。其餘是一個 192 bytes 為週期的陣列，重複放兩個
共用指標（約 42 份）。所以**不管有幾位玩家，manager 只記一個 record**。

### 唯一的路：偵測器的 `source`

每個原本的偵測器開頭都是同一段（`822C9050` 以及其餘各個）：

```asm
lwz r3,0(r4)    ; source 裡放著那位玩家的物件
lwz r11,0(r3)   ; 物件的 vtable
lwz r10,4(r11)  ; vtable[1]
bctrl           ; → 那位玩家的 body record，在 r3
```

遊戲自己就是這樣替**每一位**玩家取得身體的，而我們的覆寫完全忽略 `source`，
所以兩位玩家會拿到同一副手把的動作。`body_of_source()` 把這段包起來，
`SFR_RACE_BODY_SOURCES=1` 會印出偵測器被問到的每個不同 record：

```text
RACE_BODY_SOURCE detector=0x822c9050 source=0x... record=0x... ours=0x71730000
                 original=0x... count=1 distinct=1
```

### 答案：每位玩家自己有一個欄位，只是都被填成同一份

`reader=` 印出來的虛擬函式是 `82918418`，它整個只有兩行：

```asm
lwz r3,4(r3)   ; return object->field_4
blr
```

也就是說**record 的指標存在每個玩家物件自己的 `+4`**，不是去讀 manager 的
`+0x78`。所以每位玩家**可以**有各自的身體——遊戲只是把它們全部填成
`+0x78` 那一份，而我們又把那一份換成了自己的，於是所有玩家都讀到同一個。

證據：即使是單人 Free Race，偵測器也被問了**兩個**玩家物件
（`0xff65bb40`、`0xfef28910`），兩個的 `+4` 都等於我們的 `0x71730000`。
加上 `SFR_TWO_PLAYERS=force-racing` 之後一樣是兩個物件、同一份 record。

**所以 2P 的比賽操作是做得出來的**，做法是：

1. 每位玩家一份 `RaceInput`，各自從自己的手把更新
2. 各自一份 body record（像 `body_address` 那樣自己映射一頁就好，
   不必去找遊戲原本替第二位玩家準備的那份）
3. 每格把每位玩家物件的 `+4` 指向他自己那一份——玩家物件可以從偵測器的
   `source` 取得（`object = [source]`）
4. 偵測器的覆寫照 `source` 回答，而不是現在的單一全域 `pad_racing()`

比賽中真正被問的偵測器只有六個：`822c9050`（跳）、`822c9180`（飛行技）、
`822c9938`、`822c9a80`（加速）、`822cd068`、`822cd3f0`，不是全部 25 個。

### 但擋路的不是這裡，是選單

上面那條路要能做，前提是遊戲真的**開了一場兩人的比賽**。而目前做不到：

- 一場**單人** Free Race 就已經有兩個玩家物件（`0xff65bb10`、`0xfef28910`），
  兩個的 vtable、`+4`、`+40`、`+48` 全部相同，差別只在 `+24`/`+28`（像 id）
  與 `+100`/`+104`。**沒有看起來像玩家編號的欄位**，所以無法從物件本身
  判斷它屬於誰。
- `SFR_TWO_PLAYERS=force-racing`（比賽中才加入第二位玩家）之後，物件數量
  **還是兩個**。所以那兩個不是兩位玩家，是同一位玩家的兩個偵測器 context，
  中途加入的玩家不會得到自己的物件。
- `SFR_TWO_PLAYERS=force`（從頭就有兩位）會讓標題畫面跳出 Omochao 的教學
  詢問對話框（✓／✗），而那個對話框**只認手**、語音無效，我們的
  `823EF348` hook 也沒有被呼叫（記錄裡沒有 `NUI_DIALOG_CHOICE`），按 A／B
  都過不去。手動操作時使用者遇到的是同一道牆：2P 角色選單上，右搖桿變成
  第二位玩家的游標，第一位玩家反而不能動，於是誰都無法確認。

**所以下一步不是比賽的身體，是選單怎麼把輸入分給兩位玩家。** 線索是每位
玩家的狀態存在一個 20 bytes 間距的陣列裡（identify 的 context 分別是
`0x701346bc` 與 `0x701346d0`，就在 nui box `0x701347e0` 下面）；以及語音
辨識只有一份，一個 `ok` 到底算誰的由遊戲決定。

### 無人看管地從開機跑到比賽

`SFR_NUI_HAND_CENTRED=1` 加上 `SFR_SAY`，整條路都不需要人：

```bash
SFR_NUI_HAND_CENTRED=1 SFR_SKIP_MOVIES=1 SFR_PROFILE=1 SFR_ALLOW_RENDER_TARGETS=1 SFR_SAY="ok@2200,ok@2600,ok@3000,start@3400,ok@3800,right@4600,right@5000,right@5400,right@5800,right@6200,ok@6600,ok@7400,ok@8200,ok@9000,ok@9800,ok@10600,ok@11400" out/build/host/sfr_cpu_diagnostic.exe out/recomp/image-loader private/assets --game-region=ntsc-us
```

三件先前不知道的事：

- **標題畫面要的是 `ok`，不是 `start`**，而且要說好幾次才過（前幾次落在
  畫面還沒準備好的時候）。
- **手必須在畫面中央**，游標才會停在 START 上。`identify()` 刻意把手停在
  左上角（游標壓在按鈕上等久了就等於按下去），所以無人看管的執行需要
  `SFR_NUI_HAND_CENTRED=1`。
- **十字鍵在選單裡沒有作用**，環是靠語音詞 `right`／`left` 轉的。

主選單環有六項，`right` 一次轉一格：World Grand Prix → Time Attack →
Tutorial → 2 Player Team → Tag Race → **Free Race** → 繞回。Free Race 之後
是規則（Normal Race）、賽道（Dolphin Resort）、角色、裝備，各按一次 `ok`；
上面那串到 present 約 12000 時比賽已經在跑。

### 為什麼舊的腳本不行了

[pad-menus.md](pad-menus.md) 記的那組（`start@3+0.2,b@21+0.2,...`）過不了
標題畫面：它說的是 `start`，而標題要的是 `ok`；而且它靠十字鍵轉環，那在
選單裡沒有作用。上面那串取代它。
