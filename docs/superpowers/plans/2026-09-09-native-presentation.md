# Native game device and presentation plan

Use subagent-driven-development for isolated native presentation implementation, followed by spec and quality review. The existing autonomous authorization covers this next dependency of the same original title/menu objective.

Actual input evidence: out/graphics-parameters-boot.log records31 BE words at83E53CA8: width1280,height720,color18280186,onebuffer,noMSAA,depth enabled,depth1A220197. Reference Marathon format enums identify A8R8G8B8 and D24FS8; use native BGRA8 color and D32_FLOAT_S8_UINT depth/stencil. Host logical color channels are preserved; raw original resource uploads still require their own validated format conversion. Backbuffer/presentation texture original format28280106 is LE_X8R8G8B8.

- [x] Native presentation (agent owns src/native_presentation.h/.cpp and tests/native_presentation_test.cpp only): RAII Windows HWND, actual Plume swapchain, offscreen color/depth textures and framebuffer, command list/fence. NativeGraphics outlives it. Fixed-size client area, window initially hidden and shown only by actual Present, close messages recorded safely, no fake UI. Window creation failures/native D3D12 object handles checked before publication. No original game bytes or pointers involved.
- [x] Native API: NativeClear struct contains bool color/depth/stencil, array<float,4> color_value, float depth_value, uint8_t stencil_value. NativePresentation(NativeGraphics&,uint32_t width,uint32_t height); noncopyable; width()/height(), color()/depth() returning actual Plume textures; clear(const NativeClear&,span<const plume::RenderRect>={}); readback_color() returning tightly packed BGRA bytes; present(); pump_events(); close_requested(); window_handle() opaquevoid*. Public header can forward-declare Plume types. Validate dimensions1..8192, color/depth finite and supported depth0..1, explicit rectangle bounds before mutation. Commands synchronous for this initial correct path. No resize/format fallback.
- [x] Agent tests: meaningful RED then realGPU GREEN. Hidden window exactdimensions; invaliddimensions fail beforewindow; two distinct full clears and bytewise GPU texture readback (not CPU-filled output); rectangle clear preserves pixels outside; clear depth/stencil executes on the actual attachment; real swapchain nonempty and nativehandlesvalid. Test does not display substitute game content and does not assert title acceptance. A diagnostic test may call Present only with its own hidden window kept hidden through an explicit test setting if needed; otherwise verify actual swapchain creation and resource path without visible demo.
- [x] Root integration: CMake/native tests/build script. Establish native FreeRiders guest adapter at verified six-argument824F4CF0 using strong replacement of the weak generated public symbol; capture entry/parameters before replacement. Validate exact currently supported normal mode and complete presentation block/output bounds before native side effects. Allocate actual guest device backing and stable handles tied to native color/depth resources; unsupported guest resource field access remains explicit. Populate independently verified device offsets and render/sampler metadata without calling original GPU-driver setters. Native success only after real presentation resources and guest output publication succeed; unsupported modes/formats/states stop explicitly. Preserve original game caller/control flow.
- [x] Root clear integration:824F6DC8 verified raw PPC wrapper accepts device r3,rectcount r4,rectarray r5,flags r6,packedARGB r7,depth f1,stencil r9. It converts packed color thencalls824F6CA0,usingflags15forcolor targets,16depth,32stencil pending internal flag confirmation. Validate zero/list semantics, known device/attachment and rectangles before issuing actual native clears; no dropped draws or success-only driver imports. Continue original guest boot and preserve exact nextstop evidence.
- [x] Root final verification: realGPU test and whole CTest/Python; original entry creates native presentation resources and calls genuineclear; no game frame/title claimed until actualdraw+Present and inputvalidated. Independent reviews, source/dependency evidence, local checkpoint.

Research runs in parallel only on independent raw-PPC clear/state contracts. Shader conversion artifacts from prior turn remain evidence; production shader cache integration is the next required draw dependency. Goal remains full original title→main-menu behavior, never the native window or a clear operation alone.

Completed bounded checkpoint: original entry creates actual1280x720resources and submits packedClear; nextstop824E96C8 has observed reverseddepth1→0. Both components passed separate spec/quality reviews. Full19/19CTest,103Python/no skips, focused4bootchecks and dependency verification passed. ActualgamePresent/title/menu remain incomplete. See docs/native-presentation.md for evidence and vendor failure-path limits.
