# Original startup handling unavailable party exports

Goal: execute the game's existing missing-party-service initialization path without fabricated functions, successful party operations, guest data patches or substitute UI.

The native XAM namespace explicitly excludes six optional party/community exports for this title/menu target: 0xAFF, 0xB00, 0xB0B, 0xB10, 0x305 and 0x30B. This is native capability policy, not a claim they are absent from Xbox XAM. Pinned Xenia's XexGetProcedureAddress writes zero and returns C0000263 when an export is absent; its error table maps C0000263 to 127. Its XexUnloadImage returns success for kernel modules without unloading or decrementing their count. The original sub_82A52C60 queries all six, clears its party bindings when one is absent and calls unload via sub_82A52BA8. These are static observations until executed.

- [x] Extend NativeModules: get_procedure_address(handle, ordinal, output) requires acquired XAM handle and one of the six numeric ordinals. Validate non-null full output word before writing zero and returning C0000263; preserve host count and guards. All other handles/ordinals/name lookups stop with no mutations, including handle zero (executable lookup not yet supported). unload_image(handle) accepts acquired native XAM only and returns zero without removing the resident namespace or changing its acquisition count. Expose pure optional module_status_to_dos_error(status), handling only C0000263 ->127; all other statuses remain unhandled. Unit behavioral RED must compile against placeholder methods then fail an assertion before implementation. Test the six failures, unknowns, invalid outputs/reservations and unchanged state.
- [x] Root integration: exact XexGetProcedureAddress82ACB70C, XexUnloadImage82ACB6FC and RtlNtStatusToDosError82ACB68C dispatch after reservation guard. Status assigned only to r3, explicit logs for absent exports and error translation. Other imports/statuses continue diagnostic stops.
- [x] Native boot regression first fails on previous binary; rebuild actual startup, record whether missing-service handling continues and tighten assertions to observed next stop. No branch bypass or arbitrary success stub.
- [x] Independent spec and quality reviews, full Python/native tests, pinned dependencies, documentation and local commit. The Windows title/menu goal remains unfulfilled until actual graphics and input evidence exists.

Verified: real original entry handles all six absent exports and unload, reaching1,030 function entries. Next stop is rejected sub_82211798 due unimplemented subfze at82211D68. Full94Python/12CTest and pinned dependency verification passed. Independent specification/quality reviews found no blocking findings. Intermediate reservation test setup was corrected before final native rebuild; production guards unchanged.
