# Bounded physical allocation for original startup

Observed MmAllocatePhysicalMemoryEx at82ACBA0C: flags0,size07203000,protect4,min0,maxFFFFFFFF,alignment4. Original wrapper sub_824DF2B8 tests the returned pointer and sets error8 on null. Implement actual native backing for this supported class, not a success stub.

Pinned Xenia xboxkrnl_memory.cc333-393 supplies the six32-bit arguments, pointer/null return,4KiB default pages, rounding and physical min/max bounds. Its memory.cc maps the4KiB physical virtual range E0000000..FFCFFFFF, with physical address = virtual-E0000000+1000. We implement only this view and this allocation class initially. Other aliases,64KiB/16MiB classes, cache attributes, nonzero flags and free/query imports remain explicit missing capabilities. Native backing need not be contiguous in host physical RAM; these are guest physical addresses in a native runtime.

API PhysicalMemory(GuestMemory&); allocate(flags,size,protect,min,max,alignment)->uint32_t; statistics()->GuestMemory::Usage; static arena_base=E0000000,arena_size=1FD00000, physical_offset=1000; static overlaps_arena(address,size). Supports flags0/protect4 only, alignment0 or power-of-two<=4096, current host4KiB pages (constructor uses usage(arena_base,4096) to reject incompatible host granularity). Supported requests round size to4096 and top-down allocate entire rounded range within intersected inclusive physical bounds and known arena. Alignment rounds to4096. Zero/oversized size, empty bounds, no free range, or insufficient backing budget return0 without metadata/backing mutation. Unknown flags/protection/alignment stop before mutation. Scan page candidates against actual GuestMemory availability; all sizes/bounds/rounding use64-bit arithmetic. Actual GuestMemory map makes the range zeroed and writable. Existing reserve/commit failure behavior remains explicit; unexpected OS failure stops.

No alias mapping is silently simulated or claimed. An attempted access through an unimplemented alias still fails checked guest memory. Budget counts the single native backing once. Statistics must classify arena committed bytes as title physical pages, exclude them from kernel pages, and reject category overlap. Virtual arena capacity/reserved counts remain virtual-only.

- [x] Agent owns src/physical_memory.h/.cpp and tests/physical_memory_test.cpp: meaningful compiled RED then GREEN. Cover real readable/writable zeroed backing, 4KiB rounding, distinct descending allocations, constrained inclusive physical bounds, occupied native ranges, exhaustion without mutation, budget accounting, invalid unsupported requests, non-wrapping boundary handling and overlap query.
- [x] Root adds PhysicalMemory reference to MemoryStatistics, tests physical category changes and invalid category overlap; integrates exact import after reservation guard, only r3 receives zero-extended pointer; logs original six parameters and actual result. Build wiring and real-entry RED/GREEN with next-stop evidence.
- [x] Spec and quality review, native/Python tests, dependencies, evidence/docs updates, local commit. Continue title/menu goal; allocation is not graphics completion.
