# Checked single-thread reservation instructions

Continue after3bcf787. Actual stop sub82A52F10 contains an lwarx/stwcx. increment loop and MSR save/restore, not a memcpy. Its caller82ACA578 passes82C319B0 and returns at82ACA590. The counter derives from lis-32080/addi-4484. Do not override the whole function or mark it successful.

IBM's lwarx/stwcx. instruction reference establishes a four-byte aligned indexed effective address, load plus reservation, conditional store clearing the reservation, CR0 LT/GT cleared, EQ indicating success and SO copied from XER. Reservation granularity and interference are not fully modeled here. Support only the existing diagnostic's one thread with no ordinary stores/import calls between reservation and conditional store. Any such interference stops before side effects; do not invent a cache-line granule or silently turn a required successful path into a failing loop. Other threads, interrupts and raw host-pointer modifications are outside this model.

- [x] Memory agent: src/guest_memory.h/.cpp and tests/guest_memory_test.cpp. API uint32_t load_reserved_word(uint64_t address), bool store_conditional_word(uint64_t address,uint32_t), bool has_reservation()const. Private reservation active/address/value. Both instructions require4-byte alignment and mapped writable ordinary memory, rejecting computed readonly words before sampling. Private check_store_access contains existing range/readonly validation; public check_write additionally stops reservation-interference when active. load-reserve uses check_store_access (allows replacing a previous reservation) then checked load and publishes state; failed load leaves previous reservation untouched. Conditional store checks access before mutation; no reservation ->false, live mismatched address ->reservation-address diagnostic; matching address consumes reservation and stores BE word, returningtrue. Unexpected changed word ->false and consumes reservation (raw writes are not supported concurrency). All invalid range/alignment/readonly failures preserve memory and reservation state. Any ordinary store/check_write during live reservation stops before bytes/allocator mutation; successful SC clears state before calling normal store. Tests behavior RED then GREEN: BEload/store/fulluint32, success/consumption/noloadfalse, replacement, invalid pointers/tails/readonly no-provider, interference unchanged and original SC stillworks, mismatchedaddressstop, changedwordfalse using explicit test-only backing edit, independentmemoryinstances, maxaddress.
- [x] Root generator behavior RED then GREEN: recognize only exact upstream lwarx and stwcx. complete emission blocks with matching disassembly registers and EA expressions (rA=0 means rb, otherwise 32-bit sum). Replace lwarx block with ctx.rT.u64=sfr::load_reserved_word(ctx,EA); replace full stwcx. CR0/CAS/SO block with sfr::store_conditional_word(ctx,EA,ctx.rS.u32). Reject remaining reservation/raw CAS emission or unrecognized reservation instructions as unsupported_reservation; raw base access elsewhere still rejected. Preserve instruction comments/labels, source inputs and all other reasons. Count only emitted load_reserved/store_conditional sites. Do not generalize to ldarx/stdcx. Retain missing/unsupported halves as explicit stops where no complete pair occurs in a function (bounded intrafunction window).
- [x] Root hook adapters validate current diagnostic PCR/thread pointer/threadID; load returns zero-extended word; SC updates CR0 only after memory operation returns, preserves XER and other context. Import dispatch with live reservation stops before side effects. Add integration RED for newly retained82A52F10 and actualnextstop, regenerate into newdiagnostic-reservations folder, inspect counts and rawpointer leftovers amongretained. Fullbuild then actualrun; no patches to game data/callers.
- [x] FullPython/nativechecks/bootstrap, specthenqualityreviews, docs/currentstop/counts andlocal source-onlycommit. Continue nextdependency.

Evidence links: https://www.ibm.com/docs/en/aix/7.3.0?topic=set-lwarx-load-word-reserve-indexed-instruction and https://www.ibm.com/docs/en/aix/7.1.0?topic=set-stwcx-store-word-conditional-indexed-instruction ; pinned XenonRecomp recompiler.cpp1189/1658 emission. Native conditional-store code must not use unaligned host atomics or raw guest pointers. A diagnostic interference stop is a limit, not an emulation of all possible reservation-loss causes.
