# System time import plan

Continue the approved sequence after TLS commit ba803d6. Apply subagent-driven-development: component/test implementer, root dispatch/docs, then independent spec and quality reviews.

Evidence: actual entry stops at KeQuerySystemTime82ACB94C, LR82A68DAC, calls102. Fixed Xenia95a5c3ee250f80c3b9d139658649d9ffb6db3eec xboxkrnl_threading.cc338-344 has void ABI and only writes if output pointer is nonzero. Its clock.cc supports querying host FILETIME without scaling. Both pinned Marathon and Unleashed imports.cpp use system_clock now, 100ns ticks and epoch difference116444736000000000. We implement unscaled host wall time; paused/scaled guest time remains future work.

- [x] Component src/system_time.h/.cpp; uint64_t filetime_from_unix_ticks(int64_t ticks) where ticks count100ns since1970. Reject values before1601 with RuntimeStop time-range before any memory write; otherwise add epoch difference without signed overflow, including INT64_MAX. API void write_system_time(GuestMemory&,uint32_t output,int64_t ticks): output0 is no-op; nonzero converts then checked BE uint64 store. API void query_system_time(GuestMemory&,uint32_t output): null no-op, otherwise sample std::chrono::system_clock::now, floor duration to signed100ns ticks and invoke write. No fake r3 return or persistent host/guest clock state.
- [x] TDD tests/system_time_test.cpp. First deterministic writer tick0 must produce exact FILETIME epoch bytes; cases1601=-116444736000000000 ->0, one100nsbefore1970, one100nsafter1970, 2000-01-01 ticks9466848000000000 ->125911584000000000, INT64_MAX -> epoch+uint64maxsigned, pre1601/INT64_MIN reject. Test null no-op (including invalid time), exact8bytes/neighbors, unaligned/cross-page valid store, unmapped/wrapping/guarded-tail failures preserve bytes. Real query bound between independent before/after host samples with generous scheduling bound, not exactvalue. Record behavior RED before implementation then isolatedGREEN.
- [x] Root integration RED for IMPORT KeQuerySystemTime output trace, exactaddress/name dispatch only, capture r3output, query with no ctx mutations, log output pointer and ticks without affecting error behavior. CMake seventhCTest and PSbuild target. Build actual diagnostic, record next stop/LR/count and time-independent integration assertions. Preserve guest code and existing supported path tests; rerun boot to check whether real time changes stopping behavior.
- [x] Full82Python/sevenCTest, dependencyverify, independentreviews, accurate docs/history, local source-only commit. Continue next observed dependency.

The signed Unix-tick API is a diagnostic conversion domain, not the entire unsigned FILETIME timeline. Its maximum result fits uint64; reject the lower bound without signed negation overflow. Timestamp units express storage format, not a claim that the host clock offers100ns physical resolution. Host clock adjustments can move wall time backwards; monotonic performance timing is a separate service.
