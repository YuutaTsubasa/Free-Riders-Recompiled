# Live timestamp bundle and time base

Continue after system-time commit2a7b2a5. The real entry stops on KeTimeStampBundle82000758 at calls104. Fixed Xenia95a5c3ee250f80c3b9d139658649d9ffb6db3eec xboxkrnl_module.cc219–235 exposes a24-byte bundle and refreshes the BE uptime-milliseconds word at+16 every1ms. Only that word is read by the observed title helper824D1B98. The subsequent helper824DAEE0 executes mftb; upstream emits raw host __rdtsc(), whose frequency is not the reference runtime's49,875,000Hz. Both pinned Marathon and Unleashed KeQueryPerformanceFrequency return49875000.

Design: expose only the confirmed bundle word using checked computed reads, sampling a monotonic host clock at read time. Unknown bundle fields remain guarded/unmapped. The same clock supplies an explicit49.875MHz diagnostic time-base profile and performance frequency. This avoids a racing host timer mutating guest bytes, and supports platforms without x86 RDTSC. Sampling-on-read is a diagnostic implementation choice rather than a claim about exact kernel timer scheduling.

- [x] Independent memory component (agent): extend GuestMemory with add_read_only_word(uint32_t,std::function<uint32_t()>), check_write(uint64_t,uint64_t), and private read_scalar used by load<T>. Require registered4-byte words to be aligned, mapped, unguarded, nonoverlapping, nonempty provider; cap16 providers. Registration errors RuntimeStop memory-provider. Existing check/read guards apply before invoking any provider. Snapshot each overlapping provider once per scalar load, merge its BE bytes with ordinary backing bytes, support widths1/2/4/8 and unaligned/partial reads. No provider is invoked for failed reads or stores. Stores reject any overlap with memory-readonly before mutation. Registered computed values are a read view; backing bytes remain unchanged. Test changing reads/single-snapshot/overlap/endian/bounds/ordinary paths, source guard failures, allregistrationfaults, providerthrow, writes unchanged. TDD RED then GREEN in tests/guest_memory_test.cpp.
- [x] Independent clock/bundle component (agent after memory API agreed): src/guest_clock.h/.cpp, timestamp_bundle.h/.cpp, tests/guest_clock_test.cpp and timestamp_bundle_test.cpp. GuestClock frequency49875000; static time_base_from_nanoseconds(int64_t), uptime_from_nanoseconds(int64_t), rejectnegative clock-range, split whole seconds/remainder before multiplication, capuptimeUINT32_MAX. Methods time_base()const/uptime_milliseconds()const from steady_clock elapsed sinceconstruction. TimestampBundle(memory,std::function<uint32_t()>): base71400000, map20bytes, guard0/4/8/12, registerreadonlyword+16; rejectemptyproviderbeforemapping. No other fields exposed. Test0/subtick/exactsecond/INT64_MAX/nooverflow/saturation/realclock nondecrease; bundle dynamicbytevalues/unknownprefix/suffix/readonly/collision/providerfault. TDD; don't modify shared memory files.
- [x] Root preserve failure atomicity in existing multiwrite helpers: critical-section wholeobject validation becomes check_write; VM checks both output words writable before reserving/committing or writing either. Add regressiontests for readonly later field/output, unchanged bytes and no allocator mutation. Single-store services already check atomically; fresh mappings cannot contain registered providers.
- [x] Root generator TDD: translate only exact adjacent emitter comment `mftb rN` and matching `ctx.rN.u64 = __rdtsc();` into sfr::read_time_base(); mismatched/unrecognized raw clock intrinsic remains an explicit unsupported_time_base stop. Preserve input, labels, comments, function counts, unsupported handling. Add report time_base_reads count/clock-hook limitation. RecordRED, regenerate into new ignored diagnostic-clock directory (nooverwrite old output), inspect counts. Hook declaration in diagnostic_hooks.h; root implementation returns active GuestClock time_base or stops if absent. Exact KeQueryPerformanceFrequency82ACBAAC returns same uint64frequency in r3.
- [x] Root bind third variable __imp__KeTimeStampBundle82000758 to71400000, clock constructed before bundle callback capture. Guard remaining10 variables. Update integration trace assertions after actualrun, retain CPU/instruction validation. Build regenerated sources, fullPython/nativeCTest/dependencyverify, spec thenquality review, docs/current counts/newstop and source-only localcommit. Continue next dependency.

Scope: one diagnostic thread, read-only computed scalar view rather than full MMIO subsystem,16-provider quota, no pause/scaling/timers. Existing checked scalar hooks route all retained guest memory reads; generated raw byte/vector access still rejected. Consumers needing raw host pointers cannot use computed memory. A load spanning a forbidden field must fail before querying the clock. Avoid silently accepting mismatched mftb forms or exposing unknown bundle words as zero.
