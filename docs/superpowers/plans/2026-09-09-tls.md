# Single-thread TLS plan

Continue the approved diagnostic-to-native-port work. Use subagent-driven-development: an implementer owns the TLS component/tests; root owns boot integration and documentation, followed by independent spec and quality reviews.

Evidence: this XEX optional key `0x20104` points to raw header offset `0x35f8`: slot_count=64, raw_data_address=0x83e5f800, data_size=raw_data_size=0x9c. Fixed Xenia `95a5c3ee250f80c3b9d139658649d9ffb6db3eec` xthread.cc allocates static data followed by four-byte dynamic slots, copies raw bytes, zeros the rest, installs static pointer at PCR+0 and KTHREAD+0x68; dynamic Get/Set use big endian. kernel_state.cc uses a 2048-bit bitmap; bit_map.cc picks lowest free index. Alloc zeros the current thread slot, Free zeros slots then releases index. Marathon/Unleashed use growing host vectors; those do not establish a safe guest memory layout for this title. Current placeholder TLS has only 256 bytes before the thread object and cannot hold this title's 412 bytes without overlap.

- [x] TDD component in src/thread_local_storage.h/.cpp and tests/thread_local_storage_test.cpp. Class ThreadLocalStorage(GuestMemory&, uint32_t slot_count, uint32_t raw_address, uint32_t data_size, uint32_t raw_size), fixed static_address=0x71300000; dynamic_address()=static_address+data_size. Copy raw bytes exactly, zero static remainder and dynamic slots. Bound slots to 1..2048, static data<=65536, raw_size<=data_size; validate raw range before mapping/writes; collisions/invalid metadata fail before mutation. Map only data_size+slots*4 logical bytes. Use checked GuestMemory operations.
- [x] Methods uint32_t allocate(), free(uint32_t), get(uint32_t) const, set(uint32_t,uint32_t). Lowest free index; allocate zeroes its word before marking allocated; set returns1, get reads actual guest BE word; free clears before releasing and returns1, free(FFFFFFFF) returns0. Reject out-of-range/unallocated/double-free with RuntimeStop tls-index. If configured slots are exhausted, stop tls-capacity (do not invent console failure from diagnostic limit; Xenia's bitmap is larger than this title's storage). No host thread/scheduler/multithread claims. On guards/failures, values and allocation ownership remain unchanged.
- [x] Tests first slot0, multiple slots, lowest freed reuse zeroed, exact raw/static padding/dynamic bytes and bounds, set/get guest writes coherence, sentinel free0, invalid/double-free/capacity preserving bytes/state, config/source faults and destination collision, guard failure preserves ownership across failed allocation/free/set. Include raw range wrapping and metadata integer extremes. Record failing behavior before implementation and isolated GREEN.
- [x] Root parse existing validated module key0x20104 before guest execution, require metadata present; instantiate TLS, wire PCR+0 and thread+0x68 to static base instead of placeholder, keep thread identity address unchanged. Dispatch only KeTlsAlloc82ACBCCC, Free82ACBCDC, Get82ACBCAC, Set82ACBCBC and exact names. Check PCR identity, current-thread pointer, thread id, both TLS pointers before service; capture r3/r4 then zero-extend result into r3. Add CMake sixth CTest target and PS build target. Capture integration RED for initial metadata/Alloc trace, rebuild and assert actual next stop with LR/count. Do not change generated guest code.
- [x] Full Python/native tests, fixed-dependency check, spec then quality reviews, source-only local commit and updated accurate progress. Continue from new observed stop.

Restrictions (explicit diagnostic policy, not claimed console semantics): metadata quotas, configured slot capacity stops, allocated-slot-only Get/Set/Free, one known guest thread. No current-thread last-error side effects are added because the consulted Xbox imports do not establish them. Static TLS copying corrects previously unimplemented startup state and may expose earlier code paths; preserve all meaningful boot assertions unless real evidence changes them.
