# Native write-combined physical backing

Previous goal turn was progress: e04659a committed actual114MiB allocation and boot advanced to1267 calls. Current real stop is the second MmAllocatePhysicalMemoryEx request: flags0,size12C0,protect404,min0,maxFFFFFFFF,alignment20. Pinned xbox.h identifies400 as PAGE_WRITECOMBINE. Windows supports PAGE_READWRITE|PAGE_WRITECOMBINE on private VirtualAlloc pages; use the real host attribute, not a masked-away request.

Design: GuestMemory adds map_write_combined(address,size), supported on Windows x86-64 only. On Windows the4GiB address window uses VirtualAlloc2 placeholders; fresh host-page gaps are split and replaced by independent private reservations/commits with PAGE_READWRITE or PAGE_READWRITE|PAGE_WRITECOMBINE. Existing host commits are preserved. Reuse current bounded metadata/budget preparation, maintain a separate host-page-rounded write-combined extent registry. No attribute transition API: ordinary commit touching WC backing and WC commit touching existing backing stop before host/metadata mutation. Unsupported hosts reject before reservation. Existing reserve-plus-commit OS-failure behavior remains explicit.

All scalar store paths invoke a completion helper after writing; if intersecting WC backing, issue x86 MFENCE so checked guest stores are visible before execution continues. This is deliberately stronger ordering at the current single-thread native boundary. Normal writes have no fence. Reject reserved-word load and conditional-store to WC pages before sampling/mutation, since native interlocked operations on WC pages are not a supported path. Preserve existing reservation on rejected accesses. Other checked memory rules and unique-page budgeting remain unchanged.

PhysicalMemory accepts exact protection4 or404. It selects the corresponding real GuestMemory mapping path; all bounds/alignment/flags limits are unchanged. Track no fabricated device or GPU success. Tests inspect VirtualQuery on actual mappings, exercise zeroed/read/write/fenced stores, normal neighbors, cache-conflict rejection, budget/guard/reservation behavior, and physical allocation statistics. Future alias support must share the same backing and matching cache policy.

- [x] Agent: GuestMemory header/implementation plus existing guest_memory_test.cpp; meaningful compiled RED then native GREEN, exact VirtualQuery validation. No unrelated changes. Stable notice before root build.
- [x] Root: PhysicalMemory404 mapping selection, focused protection/output tests, runtime log and real-entry RED/GREEN with next stop; build/verification docs.
- [x] Spec and quality review, full native/Python and dependency verification; current outcome docs prepared for the local checkpoint commit. Full title/menu goal remains active.

Native probe changed the host-reservation design: plain VirtualAlloc reserveNOACCESS then commit404 reports ordinary4; reserve404 makes even later normal commits404. VirtualProtect cannot change this. out/wc-host-probe.log records all variants. out/wc-placeholder-probe.log proves adjacent4KiB placeholder replacements retain4 and404 independently and release correctly. Use dynamically resolved VirtualAlloc2 fromKernelBase.dll, fail explicitly if unavailable. Each commit prepares rollback capacity before host changes, replaces only fresh gaps, and rolls new allocations back to placeholders on failure. Splits may remain but stay owned. Destructor walks the owned4GiB partitions with VirtualQuery, computes next partition before release, and frees only bounded owned AllocationBase values. Add destruction/recommit/partition-boundary tests. This replaces the initial single-reservation implementation assumption; no host attribute assertion is weakened.

Completed evidence and the separately observed Python publication-test flake are recorded in docs/write-combine.md. Review fixes include destructor ownership bounds, skipping an already isolated placeholder split using AllocationBase, and x64-scoped WC middle-hole tests that preserve both normal neighbors. Both independent reviews approved the final component.
